<h1>Liste des unite uses</h1>

<table class="table table-hover">
    <thead>
        <tr>
            <td>#</td>
            <td>Infos</td>
            <td>Action</td>
        </tr>
    </thead>
    <tbody>
        <% @unites.each do |unite| %>
        <tr>
            <td><%= unite.id %></td>
            <td><%= unite.unitUseName %></td>
            <td>
                <a href="<%= unite_uses_path(unite.id) %>" class="fa fa-eye fa-2x"></a>
                <a href="<%= edit_unite_use_path(unite.id) %>" class="fa fa-pencil fa-2x"></a>
                <a href="<%= unite_use_path(unite.id) %>" class="fa fa-trash fa-2x" data-confirm="Etes sur sûr ?" data-method="delete"></a>
            </td>
        </tr>
        <% end %>
    </tbody>
</table>

<p>
    <a href="<%= new_unite_use_path %>" class="btn btn-success fa fa-plus-circle"> Ajouter un nouveau unite use</a>
</p>

