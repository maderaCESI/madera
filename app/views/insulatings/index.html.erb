<h1>Liste des isolants</h1>

<table class="table table-hover">
    <thead>
        <tr>
            <td>#</td>
            <td>Nom</td>
            <td>Epaisseur</td>
            <td>Prix</td>
            <td>Action</td>
        </tr>
    </thead>
    <tbody>
        <% @insulatings.each do |insulating| %>
        <tr>
            <td><%= insulating.id %></td>
            <td><%= insulating.insulatingName %></td>
            <td><%= insulating.insulatingThickness %></td>
            <td><%= insulating.priceInsulating %></td>
            <td>
                <a href="<%= insulatings_path(insulating.id) %>" class="fa fa-eye fa-2x"></a>
                <a href="<%= edit_insulating_path(insulating.id) %>" class="fa fa-pencil fa-2x"></a>
                <a href="<%= insulating_path(insulating.id) %>" class="fa fa-trash fa-2x" data-confirm="Etes sur sûr ?" data-method="delete"></a>
            </td>
            
        </tr>
        <% end %>
    </tbody>
</table>

<p>
    <a href="<%= new_insulating_path %>" class="btn btn-success fa fa-plus-circle"> Ajouter un nouvel isolant</a>
</p>

