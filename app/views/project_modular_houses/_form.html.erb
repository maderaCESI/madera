<!DOCTYPE html>
<html>
    <titre><% provide(:title, "Ajout d'un projet") %></titre>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
        
        <%= stylesheet_link_tag    'metro', media: 'all', 'data-turbolinks-track'=> 'reload' %>
        <%= stylesheet_link_tag    'metro-icons', media: 'all', 'data-turbolinks-track'=> 'reload' %>
        <%= stylesheet_link_tag    'metro-responsive', media: 'all', 'data-turbolinks-track'=> 'reload' %>
        <%= javascript_include_tag 'jquery-2.1.3.min', 'data-turbolinks-track'=> 'reload' %>
        <%= javascript_include_tag 'metro', 'data-turbolinks-track'=> 'reload' %>
        
        <link rel='shortcut icon' type='image/x-icon' href='../favicon.ico' />
    </head>
    
    <div class="app-bar fixed-top" data-role="appbar">
      <a href="<%= project_modular_houses_path %>" class="app-bar-element branding">Retour</a>
      <div class="app-bar-element place-right">
        <span class="mif-cog"></span> <%= current_user.firstname %> <%= current_user.lastname %>
      </div>
    </div>
    
    <%= form_for @projet, :html => {:class => 'align-center'} do |f| %>
        <div class="form-group">
            <%= f.label :projectName, 'Nom du projet :' %><br />
            <%= f.text_field :projectName, class: 'form-control' %>
        </div>
        
        <div class="form-group">
            <%= f.label :client_id, 'Client :'%><br />
            <%= f.collection_select :client_id, Client.all, :id, :clientName%>
        </div>
        
        <div class="form-group">
            <%= f.label :user_id, 'Commercial :'%><br />
            <%= f.collection_select :user_id, User.where(role: 'commercial'), :id, :lastname%>
        </div>
        
        <div class="form-group">
            <%= f.label :scale_modular_house_id, 'Gamme : '%><br />
            <%= f.collection_select :scale_modular_house_id, ScaleModularHouse.all, :id, :scaleName%>
        </div>
        
        <div class="form-group">
            <button class="button loading-pulse lighten primary", type="submit">Valider</button>
        </div>
    <% end %>
</html>